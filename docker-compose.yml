version: '3.8'

services:
  rgb-lightning-node:
    build: .
    ports:
      - "3001:3001"
      - "9735:9735"
    volumes:
      - rgb_data:/app/data
    restart: unless-stopped
    environment:
      - RUST_LOG=info

volumes:
  rgb_data:
    driver: local